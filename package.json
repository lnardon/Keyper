{
  "name": "keyper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "next build && next export && firebase deploy"
  },
  "dependencies": {
    "firebase": "^8.0.0",
    "next": "10.0.0",
    "next-images": "^1.6.2",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
